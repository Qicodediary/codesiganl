def isLucky(n):
    l= [int(x) for x in str(n)]
    mid=len(l)//2
    if mid!=0:
        if (sum(l[0:mid]))==(sum(l[mid:])):
            return True
        else:
            return False
    else:
        return True
    
